{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "io.github.parra28/mcp-interval",
  "description": "Read/write Intervals.icu MCP server — 55+ tools across activities, streams, wellness, calendar, gear, and sport zones, plus structured workout generation for cycling, running, and swimming.",
  "repository": {
    "url": "https://github.com/parra28/mcp-interval",
    "source": "github"
  },
  "version": "0.0.0",
  "packages": [
    {
      "registryType": "pypi",
      "identifier": "intervals-icu-mcp",
      "version": "0.0.0",
      "transport": {
        "type": "stdio"
      },
      "environmentVariables": [
        {
          "description": "Your Intervals.icu API key (from Settings → Developer)",
          "isRequired": true,
          "format": "string",
          "isSecret": true,
          "name": "INTERVALS_ICU_API_KEY"
        },
        {
          "description": "Your Intervals.icu athlete ID (e.g. i12345)",
          "isRequired": true,
          "format": "string",
          "isSecret": false,
          "name": "INTERVALS_ICU_ATHLETE_ID"
        }
      ]
    }
  ]
}
